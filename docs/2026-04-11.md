# 2026-04-11 작업 기록

## 완료 (커밋+배포)

### 1. 브리핑 빈 상태 진단 (`4c4f5a36a2`)
- `mobile.html _fetchBriefBasic`에 에러 로깅 + `_error`/`_agentId` 반환
- `_buildBriefQueue` 빈 상태 브랜치에서 에러 메시지 또는 agent_id prefix 표시
- **원인 판명**: 사용자의 agent_id = `219ecf54-6879-4636-8fb2-45ca8591c748`
  - `client_notes` 136건 중 `alert_date` 설정된 건 1건 (이미 완료됨)
  - 활성 알림/지연/예정 모두 0건 → 브리핑이 "없어요" 맞음
  - 혼란 원인: 달력이 **로그(`alert_date=null` 메모)를 "지연"으로 잘못 표시**하던 버그

### 2. 달력 "지연" 오표시 + 추가 버튼 에러 메시지 (`7b6f1426ba`)
- [hub-new/index.html](hub-new/index.html) `selectDate`:
  `past = !a._isLog && !a._isMatch && alert_date<today && !done`
- `_renderCal`의 `hasOverdue`도 동일하게 로그/매칭 제외
- `addPersonal` 에러 처리: 23502 에러 시 "DB 마이그레이션 필요" 명시
- `mobile.html`: 디버그용 agent_id 표시 제거
- 마이그레이션 파일 추가: `supabase/migrations/20260411100000_client_notes_nullable_card_id.sql`
  - 내용: `ALTER TABLE client_notes ALTER COLUMN client_card_id DROP NOT NULL`

## 미완료 / 보류

### 🔴 DB 마이그레이션 적용 필요
- 직접 DB 접속 불가 (password 로테이션 추정, pooler host 인증 실패)
- **사용자가 Supabase 대시보드 SQL 에디터에서 직접 실행 필요**
- 실행 전까지 달력 "추가" 버튼 기능 불가 (에러 메시지만 뜸)

### 🔴 손님 등록 코드리뷰 결과 (사용자 응답 대기 중)
사용자 지시: "먼저 모바일 손님 등록부터 완벽히 해결하고 브리핑으로 넘어가자"

**플로우** (mobile.html 1347~1610):
1. `showClientRegister()` → chat area 생성, AI 인사
2. 사용자 입력 → `chatSend()` → parse-property Edge Function (text prefix "손님 ")
3. 확인 카드 표시 → `chatConfirm()` → cards insert + auto-match + match-properties 병렬
4. `showClientMatchResults()` → 매칭 매물 리스트, 선택 후 카톡 공유

**발견한 버그**:
1. **[확실] `search_text_private` 미저장** — parse-property가 반환하는데 `chatConfirm`에서 저장 안 함 (line 1558~1574)
2. **[확실] 등록 성공 토스트 없음** — 화면 전환만 있음
3. **[확실] 중복 폰번호 체크 없음** — 같은 손님 여러 번 등록 가능
4. **[확실] 연락처 누락 경고 없음** — 이름/폰 없어도 통과
5. **[UX] `locate-card` 호출 없음** — 거리 매칭 보너스 0
6. **[UX] Edge Function 타임아웃 시 에러 메시지 generic**
7. **[UX] "다시 입력" 후 placeholder 예시 재표시 없음**
8. **[UX] 매칭 0건 fallback 없음** (알림으로 알려준다는 문구만)
9. **[UX] 키보드가 확인 카드 가릴 수 있음**
10. **[의문] auto-match + match-properties 중복 같지만 둘 다 필요**
11. **[의문] 손님 카드에 `trade_status:'계약가능'` 지정 — 논리적으로 이상**

**수정 대상 파일**: `mobile.html` lines 1558~1574 (chatConfirm)

**다음 세션 시작 시**: 사용자에게 "1~4번 한꺼번에 처리해도 될까?" 물어보거나, 사용자가 특정 버그 지목하면 거기부터 시작.

## 컨텍스트 메모
- 오늘 초반: 브리핑 속도/빈 상태 이슈 장시간 작업 (이전 세션부터 이어짐)
- 근본 원인: `.msg { opacity:0; animation:forwards }` + 삼성 접근성 "애니메이션 제거" 옵션
  → 이미 이전 세션에서 수정 커밋 완료
- 오늘 말미에 사용자가 "CRM 혁명 어렵다" 감정 표현 → 격려 + 방향 정리
- 사용자 모토: "광고비 0원, AI 매칭 부동산" — 오늘 발견한 작은 버그들이 혁명의 실질 재료

---

## 추가 작업 (오후~저녁, 미커밋)

### 3. 손님 등록 채팅 UX 마찰 제거 (대량)
사용자 "여기까지는 기가 막히네" 확정 → 8원칙 메모리 저장 (`feedback_chat_registration_ux.md`)
- **타이핑 라벨 상태 기반** — `_typingLabelText()`: idle="듣고 있어요" / waiting="분석 중이에요"
- **카톡 스타일 인디** — 입력 중엔 AI 인디 숨김, 전송 후에만 표시
- **끊어치기 디바운스** — `_chatBuffer` + 0.9초 `_CHAT_DEBOUNCE_MS`, 파싱 중 버퍼 쌓이면 while 루프로 이어서 처리
- **"다시 입력" → "수정"** — `chatEdit()`는 draft 유지, `_mergeDraft`로 추가만 받기. 전체 리셋은 "처음부터/리셋/취소" 키워드
- **확인 카드 중 추가 입력** — 카드 떠 있어도 `chatSend`에서 자동으로 idle 복귀 + 병합 재파싱
- **누락 "없음" 힌트 중복 제거**
- **onLoggedIn flicker 가드** — `_loggedInFired` + chatExists/panelShown 체크로 switchTab 재실행 방지
- **addTyping 비파괴** — `_lastSender` 불변 처리로 사용자 메시지 뒤 AI 응답 아바타 복원
- **알림 소리 (Web Audio)** — `playNotifySound()` 2-note sine, localStorage 토글, 내정보 패널 체크박스. 진동/무음은 OS가 처리
- **비홈 탭 results 숨김** — `results.style.display='none'`로 빈 div 패딩 공간(~106px) 제거

### 4. 매칭 카드 완전 재설계 ([mobile.html:2107~])
참고 이미지(수평 덴시티 + 태그 + 썸네일) 스타일 적용
- **"상세" 버튼 세로 렌더링 fix** — `flex-shrink:0; white-space:nowrap; word-break:keep-all`
- **박스 overflow 보호** — `.li { overflow: hidden }` + `.li-name/.li-detail` 오른쪽 24px fade-mask
- **신규 CSS 블록** `.match-card` — 가로 레이아웃 (info-col + right-col)
  - Row1: 카테고리 뱃지(아파트 월세) · 상태(계약가능/계약중/완료) · 가격(bold) · 점수(오른쪽 끝)
  - Row2: 단지명(bold) · 위치 · 전용 · 층 (fade-mask)
  - Row3: 태그 3개 + `+N` (5색 사이클 `c0~c4`: 하늘/초록/노랑/보라/빨강)
  - Row4 (`.match-source`, 공유 매물만): 점선 구분 + `사무실명 · [공유방명]`
  - Right: 썸네일(사진 있을 때 + 사진 수 배지) + 상세 버튼 세로 스택
- **데이터 소스** — match-properties Edge Function이 이미 `_agent: {name,phone}`, `_room: 공유방이름` 붙여줌 (supabase/functions/match-properties/index.ts line 591~633). UI만 추가.

### 5. 타이핑→버블 전환 플리커 해결
**원인**: `.msg { animation: msgIn 0.3s }` — removeTyping 후 새 addAiBubble이 opacity 0에서 페이드인 → 그 사이 빈 공간
**수정**: `morphTypingToAiBubble(html)` — 기존 `#chatTyping` 노드의 `.bubble` innerHTML만 교체, id 제거. DOM 재생성 없어서 애니메이션 재실행 안 함. `_processChatBuffer`의 누락 필드 질문/재안내 경로에서 호출. 확인 카드(요약 여러 버블)는 기존 `removeTyping()` 유지.

### 6. 컨테이너/패널 여백 구조적 축소
**진단**: `.container` base는 `padding: 0 24px`, active는 `padding-top`만 override → 좌우 24px 잔존. `.panel-center` 10px 추가 → 총 좌우 **34px**, 상 12px
**수정**:
- `.container.active { padding: 0 }` — 완전 제거
- `.panel.panel-center { padding: 8px 6px 80px }` — 좌우 6px만 (탭 여유)
- `.match-card { margin: 0 0 8px }` — 네거티브 제거
- 결과: **좌우 34→6px, 상 12→8px**

### 미커밋 변경 파일
- `mobile.html` — 위 3~6번 전부
- 이전 세션 잔존: `docs/next-session.md`, `seed_test_clients.py` (신규), `backup_apartments_busan_20260410_171917.json`, `fix_empty_jibun.py`, `fix_sigungu_dryrun.py` 등

**사용자 요청 시 커밋 필요** — 아직 `git commit` 안 했음

---

## 저녁 세션: 4일 장기 세션 회귀 복구 + 오피스텔 정책 재확인

### 7. `danji/style.css` 복구 (`077abf597f`)
- 이전 커밋 `a92757368c`에서 159줄 → 8줄로 손상됨
- `git show a92757368c^:danji/style.css`로 복원 (9,751바이트, 150줄)

### 8. `ranking.html` 중복 콘텐츠 제거 (`dc7f786b85`)
- 232줄 레거시 SPA → 32줄 리다이렉트 셸
- `?region=seoul&type=price` 등 쿼리 파싱 후 `/ranking/{region}-{type}` 로 `location.replace`
- canonical `/ranking/`, robots `noindex, follow`

### 9. noindex 6개 파일 추가 (`424c354ed8`)
- `my_cards.html`, `admin.html`, `card_generator_v2_auth.html`, `e2e_test.html`, `search_test.html`, `kakaomap-route-test.html`

### 10. privacy.html / terms.html SEO 메타 보강 (`3f71111f96`)
- description, robots=index, canonical, og:* 8줄 추가

### 11. `danji/app.js` 복구 (`ad893879ab` — CRITICAL)
- 역시 `a92757368c`에서 902줄(41,876바이트) 삭제돼 있었음
- 증상: 13,000개 danji 페이지가 SEO fallback만 표시, 탭·차트·역·학교 전부 공백
- `git show a92757368c^:danji/app.js`로 전체 복원
- Playwright 검증: pyeong-btn(4), canvas(2), station-name(6) 등 리치 UI 정상

### 12. `verify_browser.py` 도입
- Playwright+Chromium으로 JS 실행 후 최종 URL + DOM 마커 확인
- 허브 4 + gu 6 + dong 6 + danji 12 + ranking 6 + shell 4 + depth-1 27 = 65개 전부 PASS
- HEAD 200 OK 체크로는 "서버 응답은 정상인데 브라우저에선 리치 UI 비어있음" 상황 못 잡음을 실증

### 13. 장기 세션 회귀 방지 memory 추가
- `memory/feedback_long_session_regressions.md` 신규
- 4 규칙: 커밋 스코프 검사 / 배포 후 브라우저 검증 / 단일 목적 커밋 / 긴 세션 자각

### 14. 오피스텔 전용 정책 재확인
- 사용자가 긴 세션 중 OK 했던 결정이 흐린 판단이었을까 우려 표명
- `284f752673` 감사: 오피스텔 danji HTML 3,374개 삭제, `build_danji_pages.py`에 `id not.like.offi-*` 필터 추가
- 진짜 트리거는 offi danji→dong broken link 1,939건 — 정공법 대신 삭제 선택
- **2026-04-11 재확인**: "아파트로 안정화 먼저, 안정되면 오피스텔 확장"
- `memory/project_apartment_only_policy.md` 업데이트 (영구 삭제 아닌 단계적 도입 전략)
- 복구 자산: `284f752673^`에 오피스텔 HTML 전부 보존됨

### 오늘 저녁 배운 것
- 긴 세션 피로로 파일 경계 감각 흐려지면 관련 없는 파일이 커밋에 섞여도 감지 못 함
- HEAD 200 OK ≠ 브라우저에서 리치 UI 정상 렌더 — Playwright 필수
- "빠른 우회"가 "정공법"으로 보일 때 특히 경계 (오피스텔 삭제 건처럼)
