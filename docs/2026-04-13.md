# 2026-04-13 작업 기록 (오후 세션)

## 완료된 작업

### 데이터 파이프라인
- `build_danji_from_v2.py` limit=2000 버그 수정 → limit=1000 (Supabase max)
  - 고덕 그라시움 등 대단지(5,511건) 전세 데이터 누락 문제 해결
- 전체 재집계: 13,466개 단지, 8개 지역 (약 23분)
- HTML 재빌드: danji 12,661개 / dong 929개 / gu 114개
- fetch_all_danji() 조기 종료 버그 수정 (필터 후 길이로 루프 종료 판단하던 문제)

### UI/UX
- 면적 버튼: 공급면적 소수점1자리 표시 (`83.1㎡`), "공급" 접두사 제거, 버튼 위 "공급면적 기준" 표시
- 전용/공급 토글 완전 제거 (㎡당 가격은 항상 공급면적 기준 유지)
- 주변 단지 부족 시 Supabase 위치 기반 쿼리로 5개 채우기 (fallback)

### 버그 수정
- `showSupply` 변수 제거 후 581번 줄 잔존 참조로 JS 전체 크래시 → 긴급 수정
- 브레드크럼 "서울" 하드코딩 제거 → 실제 도시명 표시 (대구 달서구 등)
- 404.html: apt- 구버전 URL → apt-redirect.json으로 신버전 자동 리다이렉트
- verify_all.py: URL디코딩 버그, danji_pages 커버리지 분모 수정

### SEO
- 구 페이지 title 중복 수정: "중구" → "부산 중구" (6개 도시 중복 해결)
- 구 페이지 meta description: 평균 매매가 + 최고가 단지명 삽입
- 랭킹 페이지: 도입 텍스트 추가, FAQ 1→3개 (1~3위 단지)
- sitemap: apt-* 제외 + 실제 HTML 파일 기준 (12,358→12,661개, 404 URL 완전 제거)
- Google SEO 가이드 4개 에이전트 병렬 평가 실시

## 결정 사항
- 면적 표시: 공급면적 소수점1자리 (중복 방지 + 크게 느껴지는 효과)
- meta description: 데이터 자동 조합으로 충분 (Claude 생성은 트래픽 늘면 고려)
- 주변 단지: ±10㎡ 없으면 면적 무관 거리순 5개

## 미완료 → 다음 세션
1. GitHub Actions sync-trades.yml 신버전 교체
2. GSC sitemap 재제출
3. danji_pages apt- 구버전 5,137개 DB 정리
4. app.js FAQ 공급 없으면 전용 fallback
5. 카카오 정적 지도
