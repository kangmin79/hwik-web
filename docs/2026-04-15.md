# 2026-04-15 작업 기록

## 빌드 실패 원인 분석 및 수정

### trade_raw_v2 쿼리 타임아웃
- **원인**: `created_at + prop_type` 컬럼에 인덱스 없어 전체 테이블 스캔 → Supabase 8초 timeout
- **수정**: `npx supabase db query --linked`로 인덱스 직접 생성
  ```sql
  CREATE INDEX trade_raw_v2_created_at_idx ON trade_raw_v2 (created_at, prop_type);
  ```

### OG 이미지 잔재 정리
- build_danji_pages.py:936 — 개별 Supabase OG img 태그 → `/og-image.png` 통일
- sync-trades.yml — 실패/완료 이메일에서 `log_og.txt` 참조 3곳 제거

---

## 빌드 실패 자동 수정 에이전트

### auto_fix.py (신규)
- Claude Haiku로 실패 로그 분석 → Python 스크립트 버그 자동 수정
- 수정 허용: `build_*.py`, `collect_*.py` 등 빌드 스크립트만
- 차단: `danji/app.js`, `danji/style.css`, `.github/workflows/`
- exit 0=수정완료, exit 2=수정불가, exit 1=에러

### sync-trades.yml 변경
- `retry_count` workflow_dispatch 입력 추가 (최대 2회)
- `permissions: actions: write` 추가 (gh workflow run용)
- build job if 조건: `!cancelled()` 만으로 완화 (1개 지역 실패해도 빌드)
- 동/구/랭킹 스텝 tee 로그 캡처 추가
- `자동 수정 에이전트` 스텝 추가 (if: failure())
- 실패 이메일에 자동 수정 결과 포함
  - 성공 시 제목: `[AUTO-FIXED]`
  - 실패 시 제목: `[FAIL]`

### GitHub Secret
- `ANTHROPIC_API_KEY` 등록 완료

---

## 빌드 검증 오류 수정

### verify_data.py
- 공급/전용 비율 허용 범위: `1.0~1.65` → `0.95~2.0`
- 기존 기준이 너무 엄격해 정상 아파트 8,923건 오탐

### sync_trades.py
- sitemap에 `dong/index.html` → `/dong/` URL 추가
- cnt<3 이지만 HTML 존재하는 동 페이지도 sitemap 등록 (priority 0.6)
- 기존에 4개 dong URL이 sitemap 미등록 상태였음

---

## 2026-04-15 수동 빌드 결과
- 실거래 수집: 844건
- HTML 생성: 18,891개 (정상)
- SEO 검증: 내부링크/회귀방지 PASS
- 데이터 검증: HTML 대조 PASS
- 미해결: PageSpeed FAIL (root 도구파일들 — SEO 무관)
