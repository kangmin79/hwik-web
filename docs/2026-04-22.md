# 2026-04-22 — 사이트 전수 정리 완료 (배포 대기)

## 배경
어제(2026-04-21 저녁2) 세팅한 재수집 경로 B를 완주. 핵심 블로커였던 `update_apartments_slug.py` PATCH 재작성부터 전수 감사·수정까지 모두 로컬에서 완료.

## 완료된 작업 순서

### 1. apartments.slug 재계산 (10,263건)
- `update_apartments_slug.py` PATCH 방식 재작성
  - 기존: POST `?on_conflict=kapt_code` upsert → INSERT 경로 타고 NOT NULL 위반
  - 변경: `PATCH ?kapt_code=eq.{kc}` 개별 row + ThreadPoolExecutor(20) 병렬
- 결과: 10,263건 UPDATE, 실패 0, 소요 21초
- 백업: `_backup_apartments_slug.json` (30,999건 스냅샷)

### 2. nearby_complex 재생성 (30,616건)
- `sync_trades.py:update_nearby`도 같은 upsert 문제 발견
- PATCH `?id=eq.{id}` + 병렬 20으로 재작성
- 결과: 30,616건 PATCH, 실패 0, 소요 67초
- 백업: `_backup_nearby_complex.json`

### 3. 전국 재빌드
- `python build_all.py --no-db --no-og` 실행
- dong 1,533 / gu 231 / danji 18,896 / ranking 72 + sitemap 갱신
- `build_danji_from_v2.py`는 `--region` 인자 필수라 `--no-db`로 스킵 (DB는 이미 직접 PATCH로 정리됨)

### 4. sitemap dong URL 중복 버그 수정 ⭐ 진짜 버그
- 증상: `sitemap_pages.xml`에 dong URL이 **두 번씩 등록** (1,529건 중복)
- 원인: `sync_trades.py:generate_sitemap`에서
  - `seen_dong_slugs.add(safe_dong_slug)` ← quote된 문자열 저장
  - `existing_dong_slugs - seen_dong_slugs` 차집합 시 existing은 raw 한글
  - 타입 불일치로 차집합이 빠지지 않아 첫 루프 dong이 두 번째 루프에서 재등록
- 수정: `seen_dong_slugs.add(dong_slug)` (raw로 통일)

### 5. 임대 HTML 일괄 삭제
- `trade_type='임대'` 단지 HTML 283건 삭제
- 이름에 "임대" 포함된 잔여 5건도 삭제 (DB trade_type은 비어있지만 이름상 임대)
- 결과: danji 18,896 → 18,608

### 6. audit_full_check.py 신규 (정밀 전수 감사)
- 동일 규칙(quote/unquote, slug_utils.make_danji_slug)로 가짜 mismatch 방지
- 최종 결과: **모든 항목 0건**, 파일↔sitemap 1:1 완전 일치
- DB orphan 2건만 잔여 (사용자 승인 → 유지)

## 최종 상태 (로컬)

### 파일/URL 개수
| 카테고리 | 파일 | sitemap | 매칭 |
|---|---:|---:|---|
| danji | 18,608 | 18,608 | ✅ 1:1 |
| dong | 1,533 | 1,533 | ✅ 1:1 |
| gu | 231 | 231 | ✅ 1:1 |
| ranking | 72 | 72 | ✅ 1:1 |
| 정적/인덱스 | - | 5 | - |
| **합계 sitemap** | - | **20,449** | - |

### sitemap 분할 규칙 (정상)
- sitemap_seoul.xml: 서울 2,958
- sitemap_metro.xml: 경기 4,281 + 인천 1,022 = 5,303
- sitemap_cities.xml: 14개 시도 10,347
- sitemap_pages.xml: dong 1,533 + gu 231 + ranking 72 + 정적 5 = 1,841

### audit_site_health.py 재실행 결과
| 카테고리 | 결과 |
|---|---|
| C. sitemap vs 실파일 | 0건 ✅ |
| D. sitemap·canonical 일관성 | 정상(.html 통일) ✅ |
| E. nearby_complex 무결성 | 임대 0 / offi 0 / empty 0 ✅ |
| F. make_slug ≠ DB.slug | **0건** ✅ (이전 4,287) |
| J. sitemap 문제 URL | 0/0/0/0 ✅ (이전 283) |
| K. noindex 실수 | 0 ✅ |

### audit_full_check.py 최종 리포트
- A. danji HTML: sido/canonical/rental/offi/officetel/noindex/tiny 모두 0건
- B. sitemap: missing/rental/offi/officetel/sido/canonical/duplicate 모두 0건
- kapt_not_in_db 2건(송파 문정/풍납)만 의도적 유지

## 배포 전 결정 대기 2건

### (a) danji 페이지 "최근 5년 내 거래 없는 면적 표시 안 함" 안내문
- 사용자 질문: 필요한가?
- Claude 권고: **삭제 추천** — 노이즈 > 가치, 네이버부동산/호갱노노 등도 이런 주석 없음
- 위치: `build_danji_pages.py` 또는 `danji/app.js` — 결정 시 바로 수정

### (b) DB orphan 2건 — 결정됨(유지)
- `서울-송파구-문정동-문정현대아파트-a10022030`
- `서울-송파구-풍납동-송파해모로아파트-a10025632`
- apartments 마스터엔 없지만 danji_pages에 실거래 있음 → SEO 기여. 유지.

## 변경된 파일 (로컬, 아직 커밋 안 됨)
- `sync_trades.py` — update_nearby PATCH 방식 + generate_sitemap dong 중복 fix
- `update_apartments_slug.py` — PATCH + 병렬 20
- `build_danji_pages.py` — APT_SLUG_MAP 소문자 키 정규화 (어제 반영)
- `danji/app.js` — nearby 렌더 slug 우선순위 (어제 반영)
- `audit_site_health.py` (신규)
- `audit_full_check.py` (신규)
- HTML 대량: danji/*.html 전수 재생성 + 임대 288건 삭제
- sitemap_*.xml 4개 + sitemap.xml 재생성

## 로컬 테스트 서버
- `python -m http.server 8765` 로 띄워둠 (세션 종료 시 자동 정리 필요)
- 접속: http://localhost:8765/
- 단지 예시: http://localhost:8765/danji/서울-강남구-개포동-디에이치퍼스티어아이파크-a10022859.html

## 다음 세션 액션
1. 위 (a) 결정 → 수정 또는 건너뛰기
2. `git add` + 적절한 커밋 메시지 분할(① 스크립트 수정 ② 대량 HTML ③ sitemap)
3. `git push` 후 GSC sitemap 재제출 (선택)
4. 24시간 후 GSC 발견 페이지 수 변동 관찰

## 주요 학습 (feedback 후보)
- **PostgREST upsert는 NOT NULL 있는 테이블에서 위험**: INSERT 경로를 거치므로 partial payload로는 실패. `PATCH ?col=eq.X` + 개별 row가 정답. 오늘 2건 발견(apartments.slug, danji_pages.nearby_*)
- **set 연산 시 원소 타입 통일**: sync_trades의 `existing - seen` 차집합이 raw vs quoted 혼용으로 완전 실패. sitemap 전체에 1,529건 중복 유발. sitemap 같은 산출물에서 sanity check(unique URL count)를 CI에 넣어도 좋음.

---

# 2026-04-22 오후 — 자동빌드 회귀 방지 + 1차 실패 복구 + 배포

## 배경
오전 작업 push 전에 사용자 우려: "내일 새벽 자동빌드가 돌면서 오늘 작업이 되돌아가지 않는지?" 전수 확인 결과 **HIGH 위험 2종** 발견 → 선제 차단 후 push.

## 오후 작업 순서

### 1. 자동빌드 회귀 위험 감사
Agent로 .github/workflows/sync-trades.yml 경로 전수 확인:
- **HIGH A**: `build_danji_pages.py`에 임대 필터 없음 → DB에 남아있는 임대 row 기반으로 HTML 재생성 위험
- **HIGH C (false alarm)**: Agent가 "build_danji_pages.py:646-649 SSR FAQ 전용면적 폴백" 지목했으나 실제 확인 시 "최근 5년 최고가" 문장일 뿐 ㎡당 가격 FAQ 아님 → 수정 불필요
- **중요 발견**: `danji_pages` 테이블에 임대 row **1,777건** 존재 (HTML 삭제는 288건뿐이었음). 내일 자동빌드 돌면 1,489건 추가 생성됐을 위기

### 2. 근본 차단 3단계 실행
- **build_danji_from_v2.py에 `is_rental(apt)` 헬퍼 + 루프 skip 추가** (이중 방어: trade_type='임대' OR 이름에 '임대')
  - `get_changed_apts`, `process_apt_list`, `process_lawd` 모두 적용
  - apartments select에 `trade_type` 컬럼 추가
- **cleanup_rental_danji_pages.py 신규** — one-shot 스크립트. apartments.trade_type='임대' + 이름 '임대' 합집합을 danji_pages와 교집합해 delete. 실행 결과: **1,777건 삭제 / 0건 실패**
- **danji/app.js** ㎡당 가격 FAQ: 전용면적 폴백(라인 645-647) 제거 → 공급면적 없으면 FAQ 자체 미생성
  - 평형 탭 하단 "최근 5년 내 거래 없는 면적 표시 안 함" 안내문도 삭제 (오전 결정 (a))

### 3. merge + push
- 원격에 4/21 github-actions 자동빌드 커밋 1개 있었음 (`d68448839f0`) → 로컬 재빌드가 이미 최신이므로 `git merge origin/main -s ours`로 흡수
- 커밋 5개 원격 반영 완료 (`2baccf148bb..d68448839f0` 경로 해소)

### 4. 1차 자동빌드 수동 실행 → FAIL (production 영향 없음, verify 게이트가 차단)
두 가지 원인:

**Phase 1: sitemap 일치 FAIL 40,866건** ⭐ 핵심 버그
- 오전 커밋 `0c2f9b3885b`에서 sitemap URL에 `.html` 확장자 추가 (canonical과 일치시키는 올바른 변경)
- 하지만 `verify_all.py:113`이 파일 인덱스를 `f.stem`(확장자 없음)으로 저장한 채 `.html` 붙은 sitemap slug와 비교 → **20,433건 양방향 전체 mismatch**
- audit_full_check.py는 .html 있는 채로 비교해서 통과했었음 (verify_all.py만 놓침)

**Phase 6: apt-redirect 타겟 누락 31건**
- 오전에 삭제한 임대 HTML 288건이 apt-redirect.json의 타겟으로 남아있음
- `danji/apt-redirect.json`은 구버전 URL(`apt-{lawd_cd}-{name}`) → 신버전 URL 매핑. generate_redirect_map.py가 만들고 자동빌드에선 갱신 안 함

### 5. fix 커밋 + 2차 실행 → 전체 PASS
- **verify_all.py Phase 1 수정**: slug에서 `.html` 제거 후 idx와 비교 (2줄 추가)
- **apt-redirect.json 정리**: 타겟 HTML 실존 체크해 31건 제거 (4,479 → 4,448)
- 2차 실행: 17지역 수집 14,989건 신규, HTML 18,608 생성, verify 6 Phase 전부 ✓ PASS, 자동커밋 push 완료

### 6. GSC / Bing 인덱싱 요청
- **GSC sitemap 재제출** → 13,858 → 20,449 URL로 갱신 대기
- **GSC "크롤링됨-색인 안됨" 2,905건 유효성 검사 시작** (오늘 콘텐츠 변경 있으므로 재평가 가치)
- **NOINDEX 2,856건은 그대로 유지** (확인 결과 구버전 `/danji.html?id=...` URL. 의도된 noindex)
- **404 296건도 그대로** (삭제된 임대, 자연 소멸 대기)
- **Bing URL Submission 100건 제출** (서울·수도권·부산·대구·인천 주요 단지)

## 최종 커밋 히스토리 (오후)
- `908d1af7645` fix(pipeline): 임대 단지 SSOT 차단 + ㎡당 가격 FAQ 공급면적 전용
- `2baccf148bb` Merge origin/main (4/21 자동빌드 흡수)
- `5a985dd51de` fix(verify): sitemap .html 확장자 처리 + apt-redirect stale 타겟 31건 정리
- (원격 자동빌드 커밋 1개 추가됨)

## 변경된 파일 (오후)
- `build_danji_from_v2.py` — is_rental() + 루프 skip + select에 trade_type
- `cleanup_rental_danji_pages.py` (신규) — 임대 row one-shot 삭제
- `danji/app.js` — ㎡당 FAQ 공급면적 전용 + 안내문 삭제
- `verify_all.py` — Phase 1 slug .html 처리
- `danji/apt-redirect.json` — 31건 제거 (4,448개 남음)

## 배포 완료 상태 (2026-04-22 저녁)
- Actions 로그: https://github.com/kangmin79/hwik-web/actions/runs/24751600626
- 배포된 HTML: danji 18,608 / dong 1,522 / gu 231 / ranking 72
- sitemap URL 20,436 (전부 200 OK, Phase 3 확인)
- danji_pages DB: 28,844 (임대 1,777 삭제 반영)

## 주요 학습 (feedback 후보 — 오후 추가)
- **audit과 verify 기준 불일치**: audit_full_check.py는 .html 있는 채로 비교, verify_all.py는 stem 기준. 두 감사 툴이 같은 규칙 쓰도록 통일 필요. 또는 간단히 `canonical URL 형식 = 파일명 + .html` 명문화.
- **`build_danji_from_v2.py`는 upsert만 하고 delete 안 함**: 소스(apartments)에 필터 추가해도 과거 danji_pages row는 stale로 남음. 필터 추가할 땐 반드시 **기존 row 정리 one-shot**도 함께. 오늘 hidden danger 1,489건 뒤늦게 발견.
- **stat-dirty vs 실제 diff 구분**: CRLF autocrlf=true 환경에선 `git status`가 수천 개 M로 보여도 `git diff --shortstat`으로 실제 변경 파일만 집계해야 함.

## 다음 세션 액션 (우선순위)
1. **내일(4/23) 새벽 1시 자동빌드 결과 확인** — 성공 이메일 오는지, Phase 1~6 전부 PASS인지
2. **며칠 후 GSC "크롤링됨-색인안됨" 재검사 결과 확인** — 수백 건이라도 "색인됨"으로 올라오면 성공
3. **Bing URL Submission 100건 색인 속도 관찰** — 남은 일일 100개 쿼터로 추가 제출 고려
4. **(선택) apt-redirect.json `{}`로 비우기** — 구 URL 백링크 없으므로 정리해도 됨. 동작 안전 확인됨
5. **백링크/콘텐츠 차별화 전략** — 기술 SEO는 이제 끝. "어떻게 첫 100명 유입?" 고민 단계
