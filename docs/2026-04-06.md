# 2026-04-06 작업 세션

## ✅ 오전 — 인천/경기 확장 + SEO

### 인천/경기 확장
- `build_apt_db.py`: 인천/경기 상수 추가, `--incheon` / `--gyeonggi` / `--gu` 옵션
- `sync_trades.py`: 버그 수정 + `--gyeonggi` / `--incheon` 플래그 추가
- 집계 로그 버그 수정 (line 1159 `or True` 제거)
- 인천 1,943개 + 경기 4,611개 수집 완료 → apartments 총 13,418개
- `dong.html` / `ranking.html` / `gu.html`: 오피스텔 필터 제거 (아파트만 표시)

### SEO (Sonnet 단독 작업)
- canonical 즉시주입, FAQPage JSON-LD, SearchAction, sitemap gu/ranking 추가
- index.html 콘텐츠 보강, fallback 콘텐츠 적용

---

## ✅ 오후 — hub-new UX 개선

### 토론 모드 금지
- 기억 저장: 토론 모드(Sonnet+Opus 번갈아) 사용하지 않음

### 상담 기록 드롭다운 제거
- 매물 선택 드롭다운 → 심플하게 메모 입력창만 남김
- 관련 컬럼 `related_card_id` 저장 로직도 null로 단순화

### 손님 필터 개선
- 거래유형(매매/전세/월세) + 종류(아파트/오피스텔/원투룸/빌라/사무실/상가) 두 줄로 분리
- 동시 선택 가능 (예: 전세 + 아파트)
- 사무실/빌라/상가 버튼 추가

### 태그 시스템 통일 (핵심 작업)
- `TAG_STYLES` + `renderTag()` 함수 한 곳에 정의
- 기존 4군데 분산 태그 정의 → 전부 `renderTag()` 호출로 교체
- CSS `.tl-tag.*` 클래스 제거 (인라인 스타일 통일)
- 상담 유형 태그 현실화:
  - 이전: 방문/계약/대출/서류/세금/정산
  - 이후: **전화/카톡/방문/매물소개/가계약/계약/잔금/메모**
- `_parseNote()` 타입 판별 로직 새 태그명으로 교체
- 손님 상태 태그 정의: 탐색중/급해요/협의중/계약완료/연락두절

### 태그 시스템 버그 수정
- `batch-generate-tags`: `private_note` select 누락 수정
- `batch-generate-tags`: 규칙 태그 ≤2개면 Claude Haiku AI 폴백 추가
- 대량 등록(fiSaveAll) 후 `batchGenerateTags()` 즉시 호출 추가
- `_memoTagKeywords`에 `신생아특례`, `신생아대출` 키워드 추가
- TAG_STYLES에 `카톡`, `문자` 태그 추가

---

## 🔄 미완성 작업

### hub-new 남은 UX 항목
1. **손님 상태 태그** (탐색중/급해요/협의중/계약완료/연락두절) — DB `status` 컬럼 추가 필요
2. **읽음 표시** — 손님이 매물 카드 열면 중개사한테 표시
3. **피드백 버튼** — 카드 하단 관심있어요/보류/별로 버튼
4. **완료 매물 기본 숨김** — 내 매물 기본 표시 = 계약가능만

### 인천/경기 실거래 초기 적재 (미실행)
```bash
python sync_trades.py --init --gyeonggi
python sync_trades.py --init --incheon
```

### gu.html 경기/인천 탭 확장 (미착수)

---

## 🚀 다음 세션 시작점

> "docs/2026-04-06.md 보고 hub-new 손님 상태 태그(탐색중/급해요 등) DB 컬럼 추가부터 이어서 해줘"
>
> 순서:
> 1. Supabase `cards` 테이블에 `client_status` 컬럼 추가 (text, default '탐색중')
> 2. 손님 목록에 상태 배지 표시 + 클릭 시 변경
> 3. 완료 매물 기본 숨김
> 4. 읽음 표시 + 피드백 버튼 (property_view.html 연동)
