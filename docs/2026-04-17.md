# 2026-04-17 SEO 작업 기록

## 완료한 것

### 1. 전국 공급면적 수집 & 반영
- `collect_pyeongs_v2.py` 병렬 실행 완료 (17개 지역, 20,840개 단지)
- `build_danji_from_v2.py --region all` → 20,487개 danji_pages 집계
- `build_danji_pages.py` → 18,895개 HTML 재빌드 → git push 완료
- etcPurps 키워드 필터 (복도/계단/대피만) → 공급면적 정확도 개선

### 2. slug 불안정 근본 수정 (404 원인 해결)
- **원인**: `build_danji_pages.py`가 매번 `make_slug(address)`로 슬러그 재계산 → address 변경 시 URL 변경
- **수정**: `APT_SLUG_MAP` — 빌드 시 `apartments.slug` 전량 로드 → DB 고정 슬러그 우선 사용
- 3곳 모두 `APT_SLUG_MAP.get(did) or make_slug(...)` 로 교체
- 내부 링크 전수검사 결과 깨진 링크 0개 확인

### 3. 404 리다이렉트 대폭 확장
**apt-redirect.json: 971개 → 3,809개**

추가된 케이스:
- **청주청원구→청주-청원구 등 2,834개**: 도시+구 슬러그 포맷 변경 (청주/창원/수원/성남/안양/고양/용인/포항 등)
- **GSC 지적 4개**: 단지명/동명 데이터 변경 케이스
  - 경기-시흥-청영동-시화아주 → 정왕동
  - 부산-영도구-동삼동-맹동경동리인 → 영동경동리인
  - 서울-도봉구-방학동-효성히이타운 → 효성하이타운
  - 대전-유성구-원내동-전창타운 → 진잠타운

### 4. 404.html 리다이렉트 로직 확장
- 기존: `-apt-` 패턴만 처리
- 수정: 단지코드(`-a숫자`)로 끝나는 슬러그도 apt-redirect.json 조회

### 5. 빌드 후 전수 조사 (check_links_http.py)
- gu 231개 전수 + danji 100개 + dong 50개 + ranking 73개
- **404 0건** 확인

---

## 검증 파이프라인 한계 (발견)

| 도구 | 방식 | 한계 |
|------|------|------|
| `check_links.py` (워크플로우) | 파일 존재 여부 | Google 캐시 구버전 URL 못 잡음 |
| `verify_browser.py` (워크플로우) | 51개 랜덤 샘플 | 샘플링으로 특정 지역 놓침 |
| `check_links_http.py` | 실제 HTTP 요청 | **워크플로우에 없음** — 수동 실행만 |

→ `check_links_http.py`를 워크플로우에 추가하면 향후 404 자동 감지 가능 (미완료)

---

## 새벽 자동 빌드 결과 (2026-04-17)
- 실거래 신규: 432건
- 단지 HTML: 18,895개 (정상)
- Sitemap: 22,265개 ↔ 실제 파일 완전 일치
- PageSpeed FAIL 85건 → 전부 root/ 앱 파일 (SEO 무관)
- 공급/전용 비율 이상 42건 → LH/행복주택 구조상 정상

---

## 다음에 할 것 (SEO)
- GSC에서 "크롤링됨-색인 안 됨" 181개 원인 모니터링 (품질 판단, 시간 해결)
- NOINDEX 제외 목록 감소 추이 확인 (dong 페이지 app.js 수정 효과)
- `check_links_http.py` 워크플로우 추가 고려
