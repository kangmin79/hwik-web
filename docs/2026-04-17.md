# 2026-04-17 SEO 작업 기록

## 완료한 것

### 1. 전국 공급면적 수집 & 반영
- `collect_pyeongs_v2.py` 병렬 실행 완료 (17개 지역, 20,840개 단지)
- `build_danji_from_v2.py --region all` → 20,487개 danji_pages 집계
- `build_danji_pages.py` → 18,895개 HTML 재빌드 → git push 완료
- etcPurps 키워드 필터 (복도/계단/대피만) → 공급면적 정확도 개선

### 2. slug 불안정 근본 수정 (404 원인 해결)
- **원인**: `build_danji_pages.py`가 매번 `make_slug(address)`로 슬러그 재계산 → address 변경 시 URL 변경
- **수정**: `APT_SLUG_MAP` — 빌드 시 `apartments.slug` 전량 로드 → DB 고정 슬러그 우선 사용
- 3곳 모두 `APT_SLUG_MAP.get(did) or make_slug(...)` 로 교체
- 내부 링크 전수검사 결과 깨진 링크 0개 확인

### 3. 404 리다이렉트 대폭 확장
**apt-redirect.json: 971개 → 3,809개**

추가된 케이스:
- **청주청원구→청주-청원구 등 2,834개**: 도시+구 슬러그 포맷 변경 (청주/창원/수원/성남/안양/고양/용인/포항 등)
- **GSC 지적 4개**: 단지명/동명 데이터 변경 케이스
  - 경기-시흥-청영동-시화아주 → 정왕동
  - 부산-영도구-동삼동-맹동경동리인 → 영동경동리인
  - 서울-도봉구-방학동-효성히이타운 → 효성하이타운
  - 대전-유성구-원내동-전창타운 → 진잠타운

### 4. 404.html 리다이렉트 로직 확장
- 기존: `-apt-` 패턴만 처리
- 수정: 단지코드(`-a숫자`)로 끝나는 슬러그도 apt-redirect.json 조회

### 5. 빌드 후 전수 조사 (check_links_http.py)
- gu 231개 전수 + danji 100개 + dong 50개 + ranking 73개
- **404 0건** 확인

---

## 검증 파이프라인 한계 (발견)

| 도구 | 방식 | 한계 |
|------|------|------|
| `check_links.py` (워크플로우) | 파일 존재 여부 | Google 캐시 구버전 URL 못 잡음 |
| `verify_browser.py` (워크플로우) | 51개 랜덤 샘플 | 샘플링으로 특정 지역 놓침 |
| `check_links_http.py` | 실제 HTTP 요청 | **워크플로우에 없음** — 수동 실행만 |

→ `check_links_http.py`를 워크플로우에 추가하면 향후 404 자동 감지 가능 (미완료)

---

## 새벽 자동 빌드 결과 (2026-04-17)
- 실거래 신규: 432건
- 단지 HTML: 18,895개 (정상)
- Sitemap: 22,265개 ↔ 실제 파일 완전 일치
- PageSpeed FAIL 85건 → 전부 root/ 앱 파일 (SEO 무관)
- 공급/전용 비율 이상 42건 → LH/행복주택 구조상 정상

---

## 다음에 할 것 (SEO)
- GSC에서 "크롤링됨-색인 안 됨" 181개 원인 모니터링 (품질 판단, 시간 해결)
- NOINDEX 제외 목록 감소 추이 확인 (dong 페이지 app.js 수정 효과)
- `check_links_http.py` 워크플로우 추가 고려

---

# 2026-04-17 오후 세션 — SEO 코드 감사 → 워크플로우 감사

## 배포 커밋 (4건, 시간 순)

### `e797f28a` — danji/app.js 런타임 메타 덮어쓰기 제거
- **발견**: 빌드 스크립트가 정적 HTML 에 실거래가 수치 포함한 풍부한 title/description/JSON-LD 를 주입했는데, app.js `loadData()` 가 이를 짧은 템플릿 문구로 덮어쓰고 있었음
  - 정적: `교동1주공아파트 · 최근 매매가 2억 500 · 전세가율 82.4% · 최근 1년 90건 거래`
  - 런타임: `1019세대 1999년 아파트 실거래가, 전세가, 시세 추이` (모든 단지 동일 포맷)
- **영향**: Google 이 JS 실행 후 색인하므로 18,895 페이지가 **중복 콘텐츠**로 색인되던 상태
- **수정**: app.js 의 128-150(메타 덮어쓰기) + 1130-1186(injectJsonLd 중복 JSON-LD) + 1287(호출) 제거
- 정적 HTML 에 이미 ApartmentComplex + BreadcrumbList + FAQPage(7Q) 있어 중복 주입이었음

### `d04b5e4f` — 404 fuzzy 리다이렉트 + slug 인덱스 + sitemap 3건 누락 수정
- verify_all.py 돌려 내부 링크 전수 검증: **깨진 링크 0건** (내부는 clean)
- Phase 1 FAIL 3건(dong/gu/ranking index) → `build_index()` 에서 `index` slug 제외로 해소
- **신규 파일**
  - `build_slug_index.py` — 폴더 스캔 → JSON 배열 생성
  - `gu-index.json` (231), `dong-index.json` (1,533), `ranking-index.json` (72)
- **404.html fuzzy 리다이렉트**
  - `/gu/천안동남구` → `/gu/충남-천안시-동남구` (score 0.76)
  - character-hit × 0.4 + LCS × 0.6, 임계 0.7
  - sessionStorage 마커로 무한 루프 방지
- sync-trades.yml 에 `build_slug_index.py` 스텝 + `*-index.json` git add 추가

### `b23a67859b` — SearchAction 제거 + 다자구 /gu/ 모호 감지
- **config.js** `WebSite.potentialAction (SearchAction)` 제거
  - Google 2024-11 sitelinks searchbox 지원 중단
  - target `/gu/{search_term_string}` literal URL 이 크롤러에 의해 404 로 잡히던 상태
  - Organization/WebSite JSON-LD 는 유지
- **404.html** `isAmbiguousPrefix()` 추가
  - `/gu/수원시` 요청을 권선/영통/장안/팔달 중 임의의 구로 보내면 안됨
  - slug 에 포함된 candidate 가 2개 이상이면 모호 → `/gu/` 인덱스 fallback

### `75ea5531c2` — 워크플로우 C1+C2+C4
- **schedule-alerts.yml** (C1+C2)
  - secret 을 `node -e "..."` 문자열 직접 보간 → `env:` 주입 + `process.env.X` 로 변경
  - `permissions: contents: none`, `timeout-minutes: 10`, `concurrency group: schedule-alerts` 추가
- **sync-trades.yml** (C4)
  - "빌드 요약 이메일 발송" 에 `if: success()` — autofix `continue-on-error` 로 실패 삼킨 뒤 "ALL PASS" 이메일 오발송 차단

## GSC 404 69건 분석
- CSV: `c:\tmp\gsc_404.csv` (사용자가 GSC Coverage-Drilldown zip 으로 제공)
- /danji/ 48건, /dong/ 18건, /gu/ 3건

### 복구 현황
- **자동 복구**: /dong/ 12건(fuzzy), /gu/ 2건(fuzzy+모호) + 1건(SearchAction 제거), /danji/ 5건(기존 apt-redirect)
- **자연 정리 대기**:
  - /danji/ 41건: 자동 매핑 시도 결과 **위험 매칭 발견** (소만9단지 → 소만5단지, 신대림 → 한신더휴, 도담마을우미린 → 도담마을휴먼빌, 태웅 → 능곡풍림아이원)
    - 숫자 토큰/브랜드 불일치는 score 0.75+ 여도 잘못된 매핑
    - 잘못된 리다이렉트보다 기본 404 화면이 UX 상 나음
  - /dong/ 5건(관철동 등): 실제 단지 0~2개뿐. 빌드 기준 cnt≥3 은 얇은 콘텐츠 방지 차원에서 올바름

## GSC NOINDEX 2,852 → 1,000 재분석
- CSV: `c:\tmp\gsc_noindex.csv`
- path 기준 고유 경로 **5개뿐**: `/danji.html` 961 + `/dong.html` 36 + `/ranking.html` 1 + 삭제 옛 slug 1 + offi- 1
- 루트 레거시 셸 3개(`/danji.html`, `/dong.html`, `/ranking.html`)는 **의도적 noindex** — 설계대로 작동
- GSC 가 쿼리스트링 변종을 별개 URL 로 카운트. 코드 문제 0건
- 자연 감소 중

## 워크플로우 감사 결과
`/.github/workflows/` 5개 파일 전수.

### 완료 (오늘 배포)
- C1, C2, C4

### 보류 (다음 세션)
- **Critical**
  - C3: `sync-trades.yml:639` autofix `git add -A *.py` 글로브 — 파일명 명시 필요. **리스크 높음** (autofix 로직 자체 건드림)
- **High (7건)**
  - H1: `sync-trades.yml:47-49` `cancel-in-progress: true` → 90분 수집이 재실행 시 죽을 수 있음
  - H2: collect-complexes 실패 알림 경로 없음
  - H3, H4: weekly-keyword-report, fix-nearby `permissions` 누락
  - H5: `fromJSON(inputs.retry_count)` 엣지 케이스
  - H6: `git pull --rebase` conflict 방어 부족
  - H7: 월 1회 대규모 수집과 국토부 API rate limit 경합
- **Medium (5건)**
  - M1: 17개 지역 반복 → matrix strategy (470줄 → 30줄, 별도 세션)
  - M2: pip 버전 미고정 (`requirements-actions.txt` 권장)
  - M3: ANTHROPIC_API_KEY build job 전체 env 범위 → autofix step 전용으로 축소
  - M4: tee 패턴 중복 (`set -o pipefail` 통합 가능)
  - M5: concurrency 4개 파일 누락

## 관찰 포인트 (24시간)
1. 새벽 1시 sync-trades 빌드 정상 완료 이메일
2. 15분 주기 schedule-alerts 정상 동작 (Actions 탭 녹색)
3. GSC: /gu/ 404 감소, NOINDEX 1,000 → 더 감소, 크롤링됨-색인 안 됨 181 변화
4. danji 스니펫에 실거래가 수치 나타나는지 (e797f28a 효과)
5. `hwik.kr/gu/천안동남구` → `/gu/충남-천안시-동남구` 리다이렉트
6. `hwik.kr/gu/수원시` → `/gu/` 인덱스 fallback

## 임시 파일 (다음 세션 재활용 가능)
- `c:\tmp\gsc_404.csv` — GSC 404 원본
- `c:\tmp\gsc_404_analysis.txt` — fuzzy 커버 시뮬레이션
- `c:\tmp\gsc_noindex.csv` — GSC NOINDEX 원본
- `c:\tmp\gsc_noindex_analysis.txt` — NOINDEX 분석
- `c:\tmp\build_danji_redirect.py` — /danji/ 자동 매핑 스크립트 (결과 위험해서 미적용)
- `c:\tmp\danji_redirect_proposal.json` — 제안 매핑 (위험 매칭 포함, 참고용)
- `c:\tmp\analyze_noindex.py` — NOINDEX 분석 스크립트
- `c:\tmp\test_fuzzy.js`, `c:\tmp\test_ambiguous.js` — 로직 테스트

## 원칙 확인 (이번 세션에서 재확인)
- **"돌아가는 시스템은 건드리지 않는다"** — autofix 로직 등 리스크 높은 부분은 관찰 후 신중
- **잘못된 리다이렉트 > 기본 404** — 자동 매핑은 확신 없으면 보류
- **얇은 콘텐츠 금지** — dong 빌드 cnt≥3 기준 유지
- **점진 배포** — 한번에 하나씩, 24h 관찰 후 다음 단계
