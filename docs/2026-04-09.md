# 2026-04-09 작업 기록

## 완료

### SEO 전수 검증 시스템 구축
- `verify_seo.py` 생성 — 4가지 검증 통합 (링크/JSON-LD/PageSpeed/Sitemap)
- 11,644개 HTML, 215,126개 내부 링크 전수 검사
- 최종 결과: 검증 1~2, 4 PASS / 검증 3은 앱 페이지만 (SEO 무관)

### 주변 단지 링크 2,236건 깨짐 수정
- **원인**: `make_slug()`에서 주변 단지의 slug를 현재 단지의 address로 생성 → 다른 시/구 단지가 잘못된 slug
- **수정**: `DANJI_SLUG_MAP` (id→slug) 사전 계산, nearby 링크에서 맵 조회
- 거래 없는 단지(페이지 미생성)도 nearby에서 제외

### 다중 구 지역 "다른 동" 링크 404 수정
- **원인**: 고양시(덕양구/일산동구) 등에서 `make_dong_slug(gu, od, first_addr)` — 현재 페이지의 first_addr 사용
- **수정**: `dong_slug_map` 사전 계산, 각 동의 올바른 slug 조회

### SEO 최종 4건 수정
- dong 페이지 `<a href>` URL percent-encoding (593개 재빌드)
- index.html meta description 150자 이내 축소 + favicon
- about.html Google Fonts 제거 + 시스템 폰트 + favicon
- 단지 페이지 fallback HTML 링크 5곳 percent-encoding

### Chart.js CDN 복구 + 거래량·실거래가 통합 차트
- 이전 Google Fonts 제거 시 Chart.js CDN도 함께 삭제됨 (danji.html 줄 24)
- danji.html + build_danji_pages.py에 `chart.umd.js` CDN 복구
- **거래량 bar + 실거래가 scatter를 하나의 combo chart로 통합**
  - x축 동일 월 기준, 거래량 막대 반투명 배경 + 실거래가 점 오버레이
  - 차트 높이 180→220px, volumeChart 변수 제거

### 폰트 렌더링 보정
- `letter-spacing: -0.02em` + `line-height: 1.6` (style.css + danji.html)
- antialiased + grayscale + optimizeLegibility 유지

### GitHub Actions SEO 자동 검증 추가
- `sync-trades.yml`에 build 후 `verify_seo.py` 실행 단계 추가
- 결과를 이메일(SMTP_SSL)로 자동 발송
- 제목: `[휙 SEO] YYYY-MM-DD 검증 결과: PASS/FAIL`

### 공급면적 수집 로직 개선 + 전체 재수집
- **화이트리스트 → 블랙리스트 전환** (sync_pyeongs.py)
  - 이전: etcPurps에 '복도', '계단' 등 8개 키워드 있어야 공용면적 포함
  - 이후: '대피소', '주차장' 등 17개 비주거 키워드 아니면 모두 포함
- **비율 검증 안전장치** 추가: 공급/전용 1.05~1.65 범위 밖이면 전용만 사용
- **전체 13,418개 재수집 결과**:
  - pyeongs 있음: 9,458 → 9,904 (+446)
  - 값 변경: 4,935개 (기타공용 혼입 제거 1,036개 + 공용 추가 672개)
  - 비율 검증 통과: 98.2%
  - 상세 분석: docs/pyeongs_analysis_20260409.md

### 기타
- index.html BreadcrumbList 마지막 항목 URL 제거
- sitemap.xml lastmod 전체 2026-04-09로 갱신 (재크롤링 유도)
- verify_seo.py Linux 호환 (Windows 전용 stdout 분기)

## 발견 사항
- Google Search Console: 242개 색인 생성, 1.13만 미색인 (3주 차 정상)
- 노출수 177, 클릭 7, CTR 4%, 평균 게재순위 9.4 (우상향 추세)
- "크롤링됨-색인 안됨" 94건 → 대규모 수정 직후 Google 재평가 과정
- 태그 사전(tag_dictionary) 테이블 존재 — 허브 검색 시스템 핵심 인프라
- **공급면적 기존 데이터에서 기타공용 혼입이 공용 누락보다 더 큰 문제였음**
- 전국 확장 시 5만+ 페이지 → 하루 3,000~5,000 트래픽 가능 구간

## 수정된 파일
- build_danji_pages.py (DANJI_SLUG_MAP, nearby 필터, Chart.js, 링크 인코딩)
- build_dong_pages.py (dong_slug_map, 링크 인코딩)
- danji.html (Chart.js CDN, combo chart, 폰트 보정)
- danji/style.css (letter-spacing -0.02em, line-height 1.6)
- index.html (meta description, favicon, BreadcrumbList)
- about.html (Google Fonts 제거, 시스템 폰트, favicon)
- sitemap.xml (lastmod 갱신)
- verify_seo.py (신규 — 전수 검증 + Linux 호환)
- sync_pyeongs.py (블랙리스트 전환 + 비율 검증)
- .github/workflows/sync-trades.yml (SEO 검증 + 이메일 단계)
