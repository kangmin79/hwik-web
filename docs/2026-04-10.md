# 2026-04-10 작업 기록

## 완료

### 태그 사전 확장
- `supabase/migrations/20260410000000_expand_tag_dictionary.sql` 생성
- 피처 태그 11개 + 동이름 91개 + 단지명/브랜드 38개 = 총 140개 추가
- DB 적용 완료, push 완료

### 손님등록 채팅 UI (카톡 스타일)
- mobile.html에 카톡 스타일 채팅 구현
- `.chat-area`를 `body`에 직접 `appendChild` (position:fixed 정상 동작)
- 하늘색 배경(#B2C7D9) 풀스크린, 탭바(60px) 위까지
- AI 버블(왼쪽, 흰색) + 사용자 버블(오른쪽, 노란색)
- `visualViewport` API로 키보드 올라올 때 채팅 영역 리사이즈
- 키보드 시 탭바 숨기고 채팅을 키보드 위까지 축소
- 확인 카드: 다크 그라데이션 → 카톡 스타일 흰 카드로 변경
  - 헤더(거래유형+카테고리)/바디(위치+조건)/연락처/버튼 4단 구조
  - 버튼도 카톡처럼 하단 가로 분할
- 카테고리 한글 변환 (officetel→오피스텔 등)
- 중개사 메시지에 대화명 불필요 (카톡과 동일 — 노란 버블이 곧 나)
- 타이핑 인디케이터: 2초 후 "분석 중이에요..." 텍스트 추가
- 탭 재진입 시 인사 메시지 중복 방지 (타이머 추적)

### 휙 아바타 통일
- property_chat.html: 보라색 그라데이션 → 다크(#1a1a2e) + 노란(#FFD43B)
- mobile.html과 동일한 브랜드 아이덴티티

### 브리핑 탭 채팅 UI
- showAlerts()도 동일하게 body에 직접 추가 방식으로 변경
- 중복 생성 방지 (alertChatArea ID 체크)

### 버그 수정
- 로그아웃 시 body에 붙은 채팅도 제거
- 카카오 로그인에 에러 핸들링 추가 (async/await + alert)
- no-cache 메타 태그 제거 (로그인 세션 유지 방해)
- 서비스워커 캐시 버전 v2→v3

### 테스트 데이터
- 손님 카드 5명 등록 (service_role 키로)

### nearby_subway/nearby_school DB 복구 (세션 2)
- **문제**: danji_pages 78%(~10,000개)의 nearby_subway/nearby_school 빈 배열
- **원인**: sync_trades.py aggregate_danji()가 nearby 미포함 → upsert로 기존 데이터 덮어씀
- **수정**: fix_nearby.py — stations/schools 좌표 거리 계산 후 개별 PATCH
  - 1차(upsert): 실패 — partial row upsert가 PostgREST에서 무시됨
  - 2차: 이전 커밋 코드로 실행 — 효과 없음
  - 3차: PATCH 코드(f641cd4) 기반 실행 — 진행 중
- SEO 영향 없음: 정적 HTML fallback에 subway/school 태그 포함

### 차트 분리형 복원 (세션 2)
- combo chart → 분리형(거래량 bar + 실거래가 scatter) 되돌림
- danji.html + danji/app.js 모두 수정 필요 (처음 danji.html만 수정해서 누락)

### 데이터 안내 섹션 추가 (세션 2)
- SEO 텍스트 영역 내 접이식(details/summary)로 배치
- 실거래가 출처, 공급면적 설명, 취소·정정 안내

### ㎡당 가격 로직 변경 (세션 2)
- 공급면적 있을 때만 ㎡당 가격 (공급면적 기준), 없으면 세대수 표시
- 주차 항목 제거, 최고층 항상 표시

### SEO 면적 표기 개선 (세션 2)
- "보유 면적(전용): 55, 60, 61㎡" 형식으로 변경

## 발견/해결된 문제

### api.hwik.kr 522/525 에러
- **원인**: 다른 세션에서 auth.ts에 수동 JWT 서명 검증 추가 → Edge Function 전체 실패
- **해결**: Supabase 공식 auth.getUser()로 되돌림

### danji.html vs danji/app.js 이중 수정 필요
- danji.html은 템플릿, 정적 페이지는 danji/app.js 로드 → 둘 다 수정해야 반영
- GitHub Actions workflow_dispatch는 실행 시점의 main 코드 사용

### PostgREST upsert 주의
- partial row upsert 시 나머지 컬럼이 null/default로 덮어씌워질 수 있음 → PATCH 사용

## 수정된 파일 (세션 2)
- fix_nearby.py (upsert → PATCH)
- .github/workflows/fix-nearby.yml (신규)
- danji.html (차트 분리, 데이터 안내, ㎡당 가격, SEO 면적)
- danji/app.js (동일)

## 미완료
- nearby 복구 workflow #3 결과 확인
- 브레드크럼 동 링크 추가 (revert됨 — 재적용 필요)
- FAQ ㎡당 가격 질문 공급면적 기준 통일
- verify_seo.py 전체 검증
- build_ranking_pages.py
- GitHub API 토큰 생성 안내
