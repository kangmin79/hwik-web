# 2026-04-14 작업 기록 (오후2 세션 추가)

## 오후2 세션 완료

### SEO 페이지 링크 전수 검사
- 전체 20,694개 HTML 파일 내부 링크 스캔
- **broken URL 25개 발견** (gu 24개 + dong 1개)

#### 버그 1: GU_SLUGS 미검증 (`46f4be567e`)
- `_has_gu_page()`가 지역 라벨만 체크 → 실 파일 없는 gu 페이지로 404 발생
- `GU_SLUGS` 세트 추가 (DONG_SLUGS와 대칭), `_has_gu_page(address, gu_url)` 수정

#### 버그 2: 빌드 순서 문제 — dong 링크 누락
- 강원/충청/전라/경상/제주/세종 약 6,100개 단지에 dong 링크 없었음
- dong 페이지 생성 전에 danji 빌드된 것이 원인 → 재빌드로 해결

### 주상복합·도시형 생활주택 표기 개선
- **태그 추가** (`2d735a223b`): H2 옆 보라색 배지 (주상복합/도시형 생활주택)
- **유형 정확화** (`55c4d228e3`): get_prop_type() → COMPLEX_TYPE_MAP 기반
  - meta description "아파트" → 실제 유형으로 수정
- **조사 버그 수정**: `주상복합로` → `주상복합으로` (_pt_ro() 헬퍼 추가)
- `danji_pages` 뷰에 complex_type 없음 → apartments 테이블 별도 조회

### 현황
- danji 18,891 / dong 1,534 / gu 232 / ranking 37
- 내부 링크 404 완전 제거 확인

---

# 2026-04-14 작업 기록 (저녁 세션 추가)

## 저녁 세션 완료

### 전국 확장 + 파이프라인 전면 재구축
- trade_raw_v2 전체 리셋 → 7,725,639건 전국 5년치 재수집
- 경남/강원/제주 단지 수집 + 공급면적 수집
- match_apt_seq: apt_seq IS NULL 필터 추가, 7,171/7,279 매칭
- match_official ±2㎡ + pm_keys 실수값 (면적 혼재 완전 방지)
- GU_PAGE_REGIONS / ALL_TWO_TOKEN_GU 전국 확장
- 세종 동 slug 중복 제거, ranking 도 단위 전체 포함
- GitHub Actions: 새벽 1시 KST / --months 3 / --changed-only --hours 24
- 배포: danji 18,891 / dong 1,533 / gu 232 / ranking 37 / sitemap 20,690
- app.js: 거래 없는 면적 탭 표시 + "거래 없음" 안내 메시지

### 핵심 버그 수정
- 서울숲아이파크 13억→39억 정상화 (구 파이프라인 혼재 버그 해결)
- build_gu_pages 경기 외 2토큰 구 혼재, ranking 도 단위 누락, /gu/ 링크 누락

---

## 오전 세션 완료

### agent.html — 중개사 홈페이지 신규
- 단지 페이지 하단 "휙 등록 매물"에서 연결 예정
- URL: `/agent.html?id=AGENT_ID` (전체) / `?id=ID&lat=LAT&lng=LNG` (단지 유입)
- 프로필: 이름, 사무소, 주소, 전화번호, 공인중개사 뱃지
- 매물 리스트: 계약가능만, 노란 카드, 보기 → 버튼
- 바텀 시트:
  - 사진 스와이프 (무한) + 탭으로 넘기기
  - 손님에게 한마디 (사진1 → 한마디 → 사진2... 순서)
  - 사진 없으면 한마디 텍스트 표시
  - 미니맵 (배지 오른쪽 72px) → 탭 시 풀스크린
  - 아파트/오피스텔은 단지명 가격 위에 표시
  - 전화 상담 + 카카오톡 문의 버튼
- 단지 유입 모드:
  - 200m 이내(같은 단지) → 1km 이내 순으로 최대 5개
  - 하단 "다른 중개사 매물 더보기" 배너
  - 주변 단지 시세 섹션 (거리순 5개 + 최근 매매가)
- profiles.kakao_chat_url 컬럼 추가 (migration)
- 강동욱 계정에 카카오 오픈채팅 URL 등록

### sw.js 개선
- mobile.html + Supabase CDN 캐시 추가
- cache-first (stale-while-revalidate) 전략으로 변경
- 캐시 버전 v3 → v4

### mobile.html 속도 개선
- getToken() 캐시 함수 추가
- 매 요청마다 getSession() 호출 제거

### telegram-webhook
- 손님 등록 완료 후 매칭 결과 메시지에 MAIN_INLINE 버튼 추가
- 매칭 오류 시 silent catch 제거 → 오류 메시지 전송
- /start 환영 메시지 텍스트 수정 (브리핑·손님·휙허브)

## 결정사항
- agent.html 헤더 디자인: SEO 안정화 후 마무리
- 텔레그램 봇: MVP 검증용으로 유지
- CRM(mobile-v6)과 손님뷰어(agent.html)는 역할 분리
- agent.html은 손님용 읽기 전용 명함 페이지

## 다음
- danji/app.js에서 agent.html 연결 (단지 → 중개사)
- SEO 페이지 점검 (GSC 확인)
- 전국 확장 단지 수집 재개
